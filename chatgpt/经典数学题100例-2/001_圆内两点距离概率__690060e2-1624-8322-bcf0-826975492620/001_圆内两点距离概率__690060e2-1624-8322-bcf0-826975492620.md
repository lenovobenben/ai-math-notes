# 圆内两点距离概率

- Conversation ID: `690060e2-1624-8322-bcf0-826975492620`

## Question

求：圆内任意两点距离小于半径的概率

## Answer

我们把圆半径记为 $R$。在圆内随机取两点（按面积均匀独立取），求它们的距离小于 $R$ 的概率 $P$。

方法 — 条件化第一个点的位置：

设第一个点到圆心的距离为 $s$（$0\le s\le R$）。第一个点按面积均匀分布，$s$ 的概率密度为

```math
f_s(s)=\frac{2s}{R^2}.
```

在给定第一个点位置 $s$ 时，第二个点与第一个点的距离小于 $R$ 等价于第二点落入以第一个点为心、半径 $R$ 的圆与原圆的重叠区域。两等半径为 $R$、中心距为 $d=s$ 的圆的重叠面积为（已知公式）

```math
A_{\text{overlap}}(s)=2R^2\arccos\frac{s}{2R}-\frac{s}{2}\sqrt{4R^2-s^2}.
```

于是在给定 $s$ 时第二点满足条件的概率为

```math
\frac{A_{\text{overlap}}(s)}{\pi R^2}.
```

对 $s$ 取期望：

```math
P=\int_{0}^{R}\frac{A_{\text{overlap}}(s)}{\pi R^2}\cdot\frac{2s}{R^2}\,ds.
```

计算上式并化简得到解析结果

```math
P=1-\frac{3\sqrt{3}}{4\pi}.
```

数值近似为

```math
P \approx 0.5865033284\ (\approx 58.65\%).
```

答：$\displaystyle P=1-\dfrac{3\sqrt3}{4\pi}\approx0.5865$。

