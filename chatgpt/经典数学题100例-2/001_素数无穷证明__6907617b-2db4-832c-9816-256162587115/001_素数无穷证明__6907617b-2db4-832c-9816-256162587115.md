# 素数无穷证明

- Conversation ID: `6907617b-2db4-832c-9816-256162587115`

## Question

用最简单的方法证明：素数有无穷多个

## Answer

最简单且经典的证明是 欧几里得证明（反证法），步骤很短：

假设相反：素数只有有限多个，记作 $p_1,p_2,\dots,p_n$。

考虑数

```math
N = p_1p_2\cdots p_n + 1.
```

显然 $N>1$。

对任意 $i\in\{1,\dots,n\}$，用 $p_i$ 除 $N$ 会有余数 1（因为 $N = k\cdot p_i +1$）。所以没有任何已知的素数 $p_i$ 能整除 $N$。

因为每个大于 1 的整数都有素因数，$N$ 必定有某个素数因子 $q$。但 $q$ 不能是 $p_1,\dots,p_n$ 中的任何一个（见第 3 步），于是 $q$ 是一个新的素数，不在原来的有限列表中。

这与“素数只有有限多个”的假设矛盾。因此素数必然无限多个。

——证毕。

（另一个等价的简单变形是取 $N=n!+1$，对任一 $k\le n$ 有 $k\mid n!$ 但 $k\nmid n!+1$，同样得到新的素因子。）

