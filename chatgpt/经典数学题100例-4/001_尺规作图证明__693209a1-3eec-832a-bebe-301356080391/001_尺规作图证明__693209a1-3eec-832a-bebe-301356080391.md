# 尺规作图证明

- Conversation ID: `693209a1-3eec-832a-bebe-301356080391`

## Question

尺规作图问题。设有一个凸四边形ABCD，内部有一个把ABCD等比例缩小并旋转一定角度的A'B'C'D'。根据不动点定理，它们会有一个点是重合的。请找到这个点。

我这里给出一种作法。
1。AB和A'B'延长，交于点E。
2。作⊙(AA'E)和⊙(BB'E)。两圆交点F即为所求。

请证明这种作法的正确性。

## Answer

这是一个经典的“旋转相似（spiral similarity）中心”的构造与证明。下面给出清晰的几何证明（中文）。

记号与已知条件。
设 $E=AB\cap A'B'$。令两圆 $(AA'E)$ 与 $(BB'E)$ 的另一个交点（不取 $E$）为 $F$。要证：$F$ 就是把四边形 $ABCD$ 通过“等比缩放并旋转”变为 $A'B'C'D'$ 的那个中心（即不动点）。

##### 1. 证明 $F$ 是把线段 $AA'$ 变为 $BB'$ 的旋转相似中心

由于 $A,A',E,F$ 共圆，弧 $AA'$ 在该圆上所对的周角给出

```math
\angle AFA'=\angle AEA'.
```

同理，由 $B,B',E,F$ 共圆得

```math
\angle BFB'=\angle BEB'.
```

但因 $A,E,B$ 共线且 $A',E,B'$ 共线，所以

```math
\angle AEA'=\angle BEB'.
```

于是得到

(1)

```math
\angle AFA'=\angle BFB'.
```

接着再看另一对角。由于 $A,A',E,F$ 共圆，弧 $F A'$ 在此圆上对点 $A$ 的周角为

```math
\angle FAA'=\angle FEA'.
```

同理由 $B,B',E,F$ 共圆，

```math
\angle FBB'=\angle FEB'.
```

又因 $E,A',B'$ 共线，得 $\angle FEA'=\angle FEB'$，从而

(2)

```math
\angle FAA'=\angle FBB'.
```

由 (1) 与 (2) 的两组角相等，三角形 $FAA'$ 与 $FBB'$ 为相似三角形（AA 相似）。于是

```math
\frac{FA}{FA'}=\frac{FB}{FB'}.
```

结合角相等（式 (1)）可知：从点 $F$ 出发对线段 $AA'$ 作缩放再旋转可得到线段 $BB'$ —— 换言之，$F$ 是把有向线段 $AA'$ 变为 $BB'$ 的旋转相似中心（中心同时给出一个确定的放缩比与旋转角）。

##### 2. 由已给条件推出 $F$ 是整体相似变换的中心

题设说明 $A'B'C'D'$ 是由 $ABCD$ 等比缩小并旋转得到的，也就是说存在一个以某点 $O$ 为中心的旋转相似变换 $S$ 使得

```math
S(A)=A',\ S(B)=B',\ S(C)=C',\ S(D)=D'.
```

由上一节，点 $F$ 已经是把 $A\mapsto A'$ 且 $B\mapsto B'$ 的旋转相似的中心。旋转相似中心对于两对对应点是不可能有两个不同的位置的（给定两对不重合点的对应，中心唯一）：若有两个不同中心则对应的两次旋转相似在两点上的作用矛盾。因此这个中心是唯一的，必有 $F=O$。

因此，$F$ 不仅将 $A$ 变为 $A'$、$B$ 变为 $B'$，还将 $C$ 变为 $C'$、$D$ 变为 $D'$。换句话说，$F$ 是把整个四边形 $ABCD$ 映成 $A'B'C'D'$ 的旋转相似的中心——也就是题目中所说的“不动点”（重合点）。

##### 结论

用尺规作图得到的交点 $F$（即 $(AA'E)\cap (BB'E)$ 除了 $E$ 的另一个交点）恰好是将 $ABCD$ 等比缩放并旋转成 $A'B'C'D'$ 的中心，故该作法正确。

（补充说明：如果把对应边换成其它一对，比如用 $CD$ 与 $C'D'$ 做同样的构造，得到的旋转相似中心会与上面 $F$ 一致；这是唯一性的另一种几何直观。）

